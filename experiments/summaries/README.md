# Experiment summaries

Small, reviewed aggregate tables may be placed here. Raw scenario-level
results belong in `../raw_results/` and are ignored by Git because formal runs
can be large. Every aggregate must retain a pointer to its immutable raw result
artifact and protocol/config versions.
